/*
 * Here will reside the implementation of each layer that is required in our
 * implementation of the neural network. For now we will support the definition
 * of a convolutional layer, a fully connected (linear) layer, a pooling layer,
 * a flattening operation, and batch normalization.*/
